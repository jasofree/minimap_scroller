{
  "name": "minimap_scroller",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 8000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.8.0"
  }
}